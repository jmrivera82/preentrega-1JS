let numeros = [3,1,2,6]

console.log (numeros)
console.log(numeros.length)

alert (numeros[2]) /****muestra el valor del índice 2 */

for (let i=0; i< numeros.length; i++)
{
	console.lg(numeros[2])
}